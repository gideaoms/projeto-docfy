{% extends 'sections/index.njk' %}

{% block content %}

  <form action="/projects/{{ project.id }}/sections/{{ section.id }}?_method=PUT" method="post">
    <textarea style="display:none;" name="content" class="mde">{{ section.content }}</textarea>
    <button type="submit" class="btn">Salvar conteúdo</button>
  </form>

{% endblock %}
